# Python
__pycache__/
*.pyc
*.pyo

# Atomic-write temp files (write_text_atomic)
*.tmp

# Local Claude session settings / logs (not shared)
.claude/settings.local.json
.claude/logs/

# Personal/site values (admin IPs, release scrub patterns) — copy local.env.example
local.env

# Editor noise
*.swp
*~
